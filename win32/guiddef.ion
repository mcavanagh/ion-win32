@foreign
struct GUID {
    Data1: ulong;
    Data2: ushort;
    Data3: ushort;
    Data4: uchar[8];
}

@foreign
typedef LPGUID = GUID*;

@foreign
typedef LPCGUID = GUID*;

@foreign
typedef IID = GUID;

@foreign
typedef LPIID = IID*;

@foreign
typedef CLSID = GUID;

@foreign
typedef LPCLSID = CLSID*;

@foreign
typedef FMTID = GUID;

@foreign
typedef LPFMTID = FMTID*;
